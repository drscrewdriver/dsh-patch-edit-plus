# 変更履歴

## 未リリース

### 追加

- git/unified diff（既定）と Codex `apply_patch` 構文（`allowCodexPatch` によるオプトイン）を受け付ける、単一のモデル公開ツール `apply_patch`。
- 形式の自動検出。認識済みだが無効化されているスタイルが届いた場合は、実行可能なヒントを返します。
- 追加 / 更新（マルチハンク）/ 削除 / 移動の各操作。削除と移動はサンドボックス対応の `ctx.shell` を通じて実行され、パスは環境変数のみで渡されます。
- オール・オア・ナッシングの二段階エンジン: 書き込み前に完全な読み取り専用検証（3 段階の許容度によるコンテキスト位置特定、ワークスペース内包、シンボリックリンク拒否、重複パス検出）を実施します。
- すべての書き込みにおける公式の write-intent dance: `fs/write-intent` ウォーターフォール → ガード付き `writeText` → `fs/observed` 発火。これにより read-before-write ゲートがパッチ書き込みにも適用されます。
- ハンク不一致時の四要素診断（ファイル + ハンクインデックス、検索開始位置、空白を可視化した期待プレビュー、実際の抜粋）と的を絞ったヒント。
- `dryRun` パラメータと `dryRunByDefault` 設定。
- Codex セマンティクスに基づくグループ化出力（追加 → 変更 → 削除）。ファイル単位の diff は `maxDiffBytes` で上限が設けられ、リプレイ可能な `presentationMeta` を備えます。
- 三層のツール名競合保護（既定は `rename` / `skip` / `fail`）。他の `apply_patch` 提供元との名前衝突が DSH の起動を壊すことはありません。
- DSH `0.1.2-rc.1` 〜 `0.1.5-rc.2` の互換性: 単一のツールオーサリングコードパスに加え、0.1.5-rc.2 のピアパッケージに対する `typecheck:0.1.5` の静的検証。設定のデュアル API フォールバック（`installSection` / `register`）。
