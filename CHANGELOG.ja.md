# 変更履歴

## [0.1.2] — 2026-09-19

### 修正

- **設定セクションが一度も登録されていませんでした。** settings 名前空間 `patch_edit_plus` はアンダースコアを含むため、`dsh-settings` の名前空間パターン(`^[a-z][a-z0-9-]*$`)に拒否され、`register()` は検証の時点でスローしていました。パネルにも `~/.dsh/settings.yaml` にも設定セクションは現れません。`patch-edit-plus` に改名しました(移行は不要です。旧セクションは存在し得なかったため)。
- **設定レイヤーからの設定変更がツールに届いていませんでした。** `setSource`/`onChange` フックは空実装で、解決済み設定はロード時に一度固定化されていました。プラグインは解決済みソースを実際に消費するようになりました: 設定のコミットごと(または旧 `register` の watch)に設定を再解決し、ツールを再登録します。まず旧登録を破棄してから再登録するため、ライブな名前への再登録で静かに改名されることはありません。`allowCodexPatch` を含むすべてのフィールドを DSH 設定パネルから変更でき、即座に反映されます。ツールの説明(利用可能なパッチ構文の一覧)も同期し、プロセスの再起動は不要です。settings サービスのデタッチ時は composition エントリにフォールバックし、再判定は冪等です(解決済み設定全体を比較)。

## [0.1.1] — 2026-09-17

### 修正

- **`workspace-write` 下ですべての書き込みが拒否されていました。** ツールが呼び出し単位の `sandboxPolicy` を渡していなかったため、強制実行するファイルシステムはスコープなしの `ctx.sandboxPolicy.resolve()` にフォールバックし、**デプロイメント**のワークスペースルート(サーバーの起動ディレクトリ)をセッション cwd の代わりに取得していました。そのため、セッションのワークスペース内に明らかにあるパスが内包チェックを通らず、`danger-full-access` セッションであっても `file access denied under workspace-write mode` を返していました(そのモードはそもそも読まれてもいませんでした)。ポリシーは呼び出しごとに呼び出し元セッションをスコープとして解決され(`resolve({ session })`)、すべての書き込みとすべての Delete/Move の shell リクエストに付与されます。ネイティブの `write`/`edit` ツールとまったく同じです。モードの両方の側面が有効になりました: セッションの `sandbox/mode` オーバーライドと、ワークスペースルートとしてのセッション cwd です。
- パス解決とフェンスが単一のルートを共有するようになりました: プランはすべてのターゲットをポリシーの `workspaceRoot`(セッション cwd へのフォールバックあり)に対して解決するため、エンジンが書き込むパスはフェンスが測定するパスと一致します。

## 未リリース

### 追加

- git/unified diff（既定）と Codex `apply_patch` 構文（`allowCodexPatch` によるオプトイン）を受け付ける、単一のモデル公開ツール `apply_patch`。
- 形式の自動検出。認識済みだが無効化されているスタイルが届いた場合は、実行可能なヒントを返します。
- 追加 / 更新（マルチハンク）/ 削除 / 移動の各操作。削除と移動はサンドボックス対応の `ctx.shell` を通じて実行され、パスは環境変数のみで渡されます。
- オール・オア・ナッシングの二段階エンジン: 書き込み前に完全な読み取り専用検証（3 段階の許容度によるコンテキスト位置特定、ワークスペース内包、シンボリックリンク拒否、重複パス検出）を実施します。
- すべての書き込みにおける公式の write-intent dance: `fs/write-intent` ウォーターフォール → ガード付き `writeText` → `fs/observed` 発火。これにより read-before-write ゲートがパッチ書き込みにも適用されます。
- ハンク不一致時の四要素診断（ファイル + ハンクインデックス、検索開始位置、空白を可視化した期待プレビュー、実際の抜粋）と的を絞ったヒント。
- `dryRun` パラメータと `dryRunByDefault` 設定。
- Codex セマンティクスに基づくグループ化出力（追加 → 変更 → 削除）。ファイル単位の diff は `maxDiffBytes` で上限が設けられ、リプレイ可能な `presentationMeta` を備えます。
- 三層のツール名競合保護（既定は `rename` / `skip` / `fail`）。他の `apply_patch` 提供元との名前衝突が DSH の起動を壊すことはありません。
- DSH `0.1.2-rc.1` 〜 `0.1.5-rc.2` の互換性: 単一のツールオーサリングコードパスに加え、0.1.5-rc.2 のピアパッケージに対する `typecheck:0.1.5` の静的検証。設定のデュアル API フォールバック（`installSection` / `register`）。
